#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n,m; cin>>n>>m;
    if (m >= (n + 1) / 2) cout<<"NEWBIE"<<"\n";
    else cout<<"PRO"<<"\n";
    
    return 0;
}